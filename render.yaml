services:
  - type: web
    name: invoice_systemnew
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PYTHON_VERSION
        value: "3.11.0"
